// Besides false, possible falsy expressions are: null, NaN, 0, the empty string (""), and undefined.
// -1 1 and any other numer evaluates to true 0 to false

let val = 0 ? true : false;
console.log(val);
